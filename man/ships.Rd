% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-ships.R
\docType{data}
\name{ships}
\alias{ships}
\title{Ship observation records}
\format{
A tibble with 12310 rows and 21 variables:
\describe{
\item{record_id}{(\code{integer}) Record identifier. Links to \code{birds$record_id}.}
\item{date}{(\code{date}) Observation date.}
\item{time}{(\code{time}) Local time at the start of the 10-minute count.}
\item{latitude}{(\code{double}) Decimal latitude (negative values indicate southern hemisphere).}
\item{longitude}{(\code{double}) Decimal longitude.}
\item{hemisphere}{(\code{factor}) East/West hemisphere: \code{"E"} or \code{"W"}.}
\item{activity}{(\code{factor}) Ship activity during the count period. One of \code{"steaming, sailing"}, \code{"dropping trash"}, \code{"trawling"}, \code{"oceanography"}, \code{"potting"}, \code{"line fishing"}, \code{"cleaning fish"}, \code{"stationary"}, \code{"flying helicopters"}, or \code{"whaling"}. Recoded from numeric codes 1--10 in the source data.}
\item{speed}{(\code{double}) Ship speed in knots.}
\item{direction}{(\code{integer}) Ship direction in degrees.}
\item{cloud_cover}{(\code{ordered}) Cloud cover: \code{"clear"}, \code{"partially cloudy"}, or \code{"overcast"}. Recoded from codes 0--2 in the source data.}
\item{precipitation}{(\code{factor}) Precipitation type: \code{"none"}, \code{"squalls"}, \code{"fog"}, \code{"drizzle"}, \code{"rain"}, \code{"showers"}, \code{"snow showers"}, or \code{"continuous snow"}. Recoded from numeric codes in the source data.}
\item{wind_speed_class}{(\code{integer}) Wind speed on the Beaufort scale (0--12). Join to \link{beaufort_scale} for descriptions and knot ranges.}
\item{wind_direction}{(\code{integer}) Wind direction in degrees.}
\item{air_temperature}{(\code{double}) Air temperature in degrees Celsius.}
\item{pressure}{(\code{integer}) Atmospheric sea-level pressure in millibars.}
\item{sea_state_class}{(\code{integer}) Sea state class (0--6). Join to \link{sea_states} for descriptions and wave height ranges.}
\item{sea_surface_temperature}{(\code{double}) Sea surface temperature in degrees Celsius.}
\item{depth}{(\code{integer}) Sea floor depth in meters.}
\item{observer}{(\code{factor}) Name of the observer, decoded from a 4-letter code in the source data.}
\item{census_method}{(\code{ordered}) Count method: \code{"partial"} indicates a count lasting less than 10 minutes or a casual observation; \code{"full"} indicates a complete 10-minute count. Recoded from \code{"P"} and \code{"F"} in the source data.}
\item{season}{(\code{ordered}) Southern hemisphere season: \code{"summer"}, \code{"autumn"}, \code{"winter"}, or \code{"spring"}. Recorded directly in the source data (but likely derived rather than being entered directly in the log book data).}
}
}
\source{
At-Sea Observations of Seabirds dataset, Te Papa Tongarewa, The
Museum of New Zealand.
\url{https://www.tepapa.govt.nz/learn/research/datasets/sea-observations-seabirds-dataset}
}
\usage{
ships
}
\description{
Environmental and operational records for each 10-minute seabird census
period, covering at-sea observations from 1969 to 1990. Each row represents
one census period and links to zero or more bird observation records in
\link{birds} via \code{record_id}.
}
\details{
Several columns derived in the source data have been excluded: the original
row order (implicit in the data), month (derivable from \code{date}), longitude in
0--360-degree format (derivable from \code{longitude} and \code{hemisphere}), 1-degree
grid cell centre coordinates (derivable from \code{latitude} and \code{longitude}), and
sea surface salinity (missing for all but one entry).
}
\examples{
ships

# Join ship records with bird observations
if (requireNamespace("dplyr", quietly = TRUE)) {
  dplyr::left_join(ships, birds, by = "record_id")
}
}
\seealso{
\link{birds} for the corresponding bird observation records. \link{sea_states}
and \link{beaufort_scale} for lookup tables describing the \code{sea_state_class} and
\code{wind_speed_class} codes. The script used to prepare this dataset:
\url{https://github.com/jonthegeek/seabirddata/blob/main/data-raw/ships.R}
}
\keyword{datasets}
