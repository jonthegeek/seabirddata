% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-birds.R
\docType{data}
\name{birds}
\alias{birds}
\title{Bird observation records}
\format{
A tibble with 49019 rows and 26 variables:
\describe{
\item{bird_observation_id}{(\code{integer}) Identifier for this bird observation record, corresponding to the original record order in the source data.}
\item{record_id}{(\code{integer}) Record identifier. Links to \code{ships$record_id}. One observation (record_id 1184009, bird_observation_id 974) has no corresponding ship record in the source data.}
\item{species_common_name}{(\code{character}) The original log entry as recorded in the source data. May encode species, age, sex, and plumage phase in a single string. \code{NA} indicates a census period with no birds recorded (converted from the sentinel value \code{"[NO BIRDS RECORDED]"} in the source data).}
\item{species_scientific_name}{(\code{character}) Scientific name, derived from \code{species_common_name} by Te Papa staff. May represent a species aggregate if the log entry could not be identified to a single species.}
\item{species_abbreviation}{(\code{character}) Abbreviated species name (mostly the first three letters of the genus and species), derived from \code{species_common_name} by Te Papa staff.}
\item{age}{(\code{ordered}) Age class, derived from \code{species_common_name}. One of \code{"juvenile"}, \code{"immature"}, \code{"subadult"}, or \code{"adult"}.}
\item{wan_plumage_phase}{(\code{ordered}) Wandering albatross plumage phase, derived from \code{species_common_name}. One of \code{"all brown"}, \code{"brown plumage breaking"}, \code{"white patch on wing"}, \code{"wing patch breaking"}, or \code{"white"}.}
\item{plumage_phase}{(\code{ordered}) Plumage phase for species other than wandering albatross, derived from \code{species_common_name}. One of \code{"dark"}, \code{"intermediate"}, \code{"light"}, or \code{"white"}.}
\item{sex}{(\code{factor}) Sex, derived from \code{species_common_name}. One of \code{"female"} or \code{"male"}.}
\item{count}{(\code{integer}) Total number of birds counted in this observation. \code{99999} is used for counts estimated to be over 100,000.}
\item{n_feeding}{(\code{integer}) Number of birds observed feeding (unspecified whether actively or passively). \code{99999} is used for counts estimated to be over 100,000.}
\item{feeding}{(\code{logical}) Whether any birds were observed feeding.}
\item{n_sitting_on_water}{(\code{integer}) Number of birds sitting on water.}
\item{sitting_on_water}{(\code{logical}) Whether any birds were sitting on water.}
\item{n_sitting_on_ice}{(\code{integer}) Number of birds sitting on ice.}
\item{sitting_on_ice}{(\code{logical}) Whether any birds were sitting on ice.}
\item{sitting_on_ship}{(\code{logical}) Whether any birds were sitting on the ship.}
\item{in_hand}{(\code{logical}) Whether any birds were held in hand (i.e., captured).}
\item{n_flying_past}{(\code{integer}) Number of birds flying past. \code{99999} is used for counts estimated to be over 100,000.}
\item{flying_past}{(\code{logical}) Whether any birds were flying past.}
\item{n_accompanying}{(\code{integer}) Number of birds accompanying the ship (flying alongside).}
\item{accompanying}{(\code{logical}) Whether any birds were accompanying the ship.}
\item{n_following_ship}{(\code{integer}) Number of birds following the ship's wake.}
\item{following_ship}{(\code{logical}) Whether any birds were following the ship's wake.}
\item{moulting}{(\code{logical}) Whether any birds were observed moulting.}
\item{naturally_feeding}{(\code{logical}) Whether any birds were naturally feeding (i.e., not feeding on ship discards).}
}
}
\source{
At-Sea Observations of Seabirds dataset, Te Papa Tongarewa, The
Museum of New Zealand.
\url{https://www.tepapa.govt.nz/learn/research/datasets/sea-observations-seabirds-dataset}
}
\usage{
birds
}
\description{
Individual seabird observation records for each 10-minute census period,
covering at-sea observations from 1969 to 1990. Each row represents one
species entry within a census period and links to the corresponding
environmental and ship data in \link{ships} via \code{record_id}.
}
\details{
The data was built from records extracted by Te Papa staff from the at-sea
bird observation logbooks of Captain John Arthur Francis Jenkins (1928--1989)
and count cards held by the convenor of the Australasian Seabird Mapping
Scheme. The logbooks of Captain Jenkins are held by the Archives of the
Auckland War Memorial Museum.

\code{species_common_name} contains the original log entry as it appeared in the
source data. The raw column was named "Species common name (taxon [AGE / SEX
/ PLUMAGE PHASE])", reflecting that a single entry could encode species,
age, sex, and plumage information together. The columns
\code{species_scientific_name}, \code{species_abbreviation}, \code{age},
\code{wan_plumage_phase}, \code{plumage_phase}, and \code{sex} were all derived from this
log entry by Te Papa staff.

For the occurrence columns (\code{feeding}, \code{sitting_on_water}, etc.), \code{NA}
indicates the occurrence was unknown or unrecorded (original value \code{"U"} in
the source data).
}
\examples{
birds

# Join with ship records for environmental context
if (requireNamespace("dplyr", quietly = TRUE)) {
  dplyr::left_join(birds, ships, by = "record_id")
}
}
\seealso{
\link{ships} for the corresponding ship and environmental records. The
script used to prepare this dataset:
\url{https://github.com/jonthegeek/seabirddata/blob/main/data-raw/birds.R}
}
\keyword{datasets}
